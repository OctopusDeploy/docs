---
/**
 * Add custom Plausible code here
 */
---

<script is:inline>
  document.addEventListener('searched', (e) => {
    if (typeof plausible != 'undefined') {
      plausible('Search', { props: e.detail });
    }
  });
</script>
